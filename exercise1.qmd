---
title: "homework"
format: html
editor: visual
---

## Quarto

Quarto enables you to weave together content and executable code into a finished document. To learn more about Quarto see <https://quarto.org>.

## Running Code

When you click the **Render** button a document will be generated that includes both content and the output of embedded code. You can embed code like this:

```{r}
1 + 1
```

You can add options to executable code like this

```{r}
#| echo: false
1+1
```

```{r}
#|llabel: 1(a-g)
#|a
p_la<- 1:20
p_la
#|b
p_lb<- 20:1
p_lb
#|c
p_lc <- c(1:20,19:1)
p_lc
#|d
p_ld <- c(4,6,3)
tem<- p_ld
tem
#| e
tem <- c(4,6,3)
p_le <- rep(tem,10)
p_le
#|f
p_lf<- c(rep(tem,10),4)
p_lf

#|g
p_lg<-c(rep(4,10),rep(6,20),rep(3,30))
p_lg
length(which(p_le==4))

tmp<- 10:100
tmp
tmp^3+4*tmp^2

Sepal.Length <- c(5.1, 4.9, 4.7, 4.6, 5.0, 5.4) 
Sepal.Width <- c(3.5, 3.0, 3.2, 3.1, 2.5, 2.6) 
Petal.Length <- c(1.4, 1.4, 1.3, 1.5, 3.0, 4.4) 
Species <- c("setosa", "setosa", "setosa", "setosa", "versicolor","versicolor") 
iris <- data.frame(Sepal.Length = Sepal.Length, Sepal.Width = Sepal.Width, Petal.Length =Petal.Length, Species = Species)
subset(iris, select = c(Sepal.Length, Species))
iris[, c("Sepal.Length", "Species")]
iris[iris$Species == "setosa", ]
subset(iris, Species == "setosa")




```

```{2 * 2}
```

The `echo: false` option disables the printing of code (only output is displayed).

```{r}
temFn <-function(a) {
  n=length(a)
  mva = (a[-c(n-1,n)]+a[-c(1,n)]+a[-c(1,2)])/3
  return mva
}
temFn <-function(mat){
  if(!is.matrix(mat)) stop("it is not a matrix")
  mat<-mat[mat%%2 == 1]*2
  return(mat)
}
A<- matrix(c(1,1,3,5,2,6,-2,-1,-3),3,3,byrow=TRUE)
# A%%2 == 0
# A[A%%2 == 1]
# re<-A[A%%2 == 1]*2
temFn(A)

testloop<- function(n){
  if(n<4)stop("n must be an integer >3")
  browser()
  x =rep(NA, n-1)
  x[1] = 1
  x[2] = 2
  
  for (j in 3:(n-1)) {x[j]= x[j-1]+2/x[j-1]}
  return(x)
  }
testloop(5)
```
